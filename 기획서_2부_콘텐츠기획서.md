# 게임 알고리즘 & AI 대회 — 콘텐츠 기획서

> **문서 구분**: 2부 / 대회 콘텐츠 기획서 (내부 출제·기술팀용)
> **작성일**: 2026년 3월
> **버전**: v0.3 (게임 규칙 확정)

---

## 1. 대회 콘텐츠 개요

### 핵심 컨셉

> "참가자들은 당일 처음 공개되는 게임을 보고, 그 자리에서 AI 에이전트를 만들어 싸운다."

- 게임 규칙은 대회 당일 공개 → 사전 준비 코드 활용 불가 (전략/알고리즘 사고력 직접 측정)
- 게임 시뮬레이터는 주최측이 사전 제작하여 당일 배포
- 참가자는 시뮬레이터 API를 호출하는 에이전트 코드를 작성
- 완성된 에이전트끼리 자동 대결로 순위 결정

---

## 2. 게임 설계: 동시가위바위보 ✅ 확정

### 2.1 게임 개요

| 항목 | 내용 |
|------|------|
| **게임명** | 동시가위바위보 |
| **유형** | 영토 확장형 3인 동시 전략 게임 |
| **맵** | 11행 정삼각형 격자 (총 121셀) |
| **플레이어** | 3명 (가위 ✂ / 바위 ✊ / 보 ✋) |
| **시작 기물** | 플레이어당 **4개** |
| **최대 기물** | 플레이어당 20개 (복제 제한) |
| **최대 턴** | 200턴 |

### 2.2 맵 구조

삼각형 격자 (Triangular Grid):
- 행 `r` (0 ~ 10): `2r+1`개 셀
- 셀 `(r, c)`: `c%2==0`이면 △(위), `c%2==1`이면 ▽(아래)
- 각 셀은 최대 **1개의 기물**만 점유 가능 (팀 구분 없이)

```
행 0:       △
행 1:      △▽△
행 2:     △▽△▽△
  ...
행 10: △▽△▽...△▽△  (21셀)
```

### 2.3 시작 배치 ✅ 확정

각 플레이어는 격자 꼭짓점에서 작은 삼각형 형태로 시작:

| 플레이어 | 위치 | 위치 설명 |
|---------|------|-----------|
| **가위 ✂** | (0,0), (1,0), (1,1), (1,2) | 꼭대기 삼각형 |
| **바위 ✊** | (9,0), (10,0), (10,1), (10,2) | 좌하단 삼각형 |
| **보 ✋** | (9,18), (10,18), (10,19), (10,20) | 우하단 삼각형 |

### 2.4 인접 관계 (이동 가능 방향)

```
△(r,c): 이웃 → (r, c-1), (r, c+1), (r+1, c+1)
▽(r,c): 이웃 → (r, c-1), (r, c+1), (r-1, c-1)
```

### 2.5 턴 구조 ✅ 확정

한 라운드는 세 단계로 진행:

```
1단계: 가위 플레이어 액션 입력
2단계: 바위 플레이어 액션 입력
3단계: 보 플레이어 액션 입력
       ↓
4단계: 세 플레이어 액션 동시 적용 + 전투 해결
```

> **핵심**: 모든 플레이어가 액션을 결정한 후 **동시에** 반영됩니다.
> 상대의 결정을 모른 채 행동해야 합니다.

### 2.6 액션 규칙 ✅ 확정

**한 턴에 플레이어는 기물 1개에 대해 1가지 행동만 가능:**

| 행동 | 거리 | 조건 |
|------|------|------|
| **복제 (CLONE)** | 1칸 (인접 셀) | 대상 셀이 비어있을 것 |
| **이동 (MOVE)** | 2칸 (BFS 반경 2) | 대상 셀이 아군 기물 없을 것 |

- **거리 1칸 = 무조건 복제**: 인접 셀 선택 시 원본은 그대로, 해당 셀에 새 기물 생성
- **거리 2칸 = 무조건 이동**: BFS 거리 2인 셀로 기물이 실제 이동
- 이동과 복제 모두 동시 처리 (스냅샷 기반)
- 복제 시 20개 한도 초과 불가

### 2.7 전투 규칙 ✅ 확정

가위바위보 상성:
```
가위 → 보 제거 (가위가 보를 이김)
바위 → 가위 제거
보   → 바위 제거
```

전투 발생 조건: **같은 셀** 또는 **인접 셀**에 다른 팀 기물이 있을 때

**동시 전투 해결 (스냅샷 방식):**
1. 이동 완료 후 모든 기물 위치 스냅샷
2. 각 기물에 대해 "내가 지는 상대"가 같은 셀 또는 인접 셀에 있으면 제거 대상으로 표시
3. 표시된 기물을 **동시에** 제거 (연쇄 효과 포함)

> 예시: 가위(A) ─ 보(B) ─ 바위(C) 일직선 배치
> - B는 A에게 지고, C는 B에게 짐
> - A는 C와 인접하지 않으므로 무사
> - 결과: B, C 제거 / A 생존

### 2.8 승리 조건

| 조건 | 결과 |
|------|------|
| 상대 기물이 모두 제거됨 | 남은 플레이어 승리 |
| 모든 기물 동시 제거 | 무승부 |
| 200턴 경과 | 기물 수 가장 많은 팀 승리 (동수면 무승부) |

---

## 3. 시뮬레이터 기술 명세 ✅ 구현 완료

### 3.1 시뮬레이터 제공 항목 (당일 배포)

- 게임 규칙서 (PDF / Markdown)
- 웹 기반 시뮬레이터 (`http://서버주소:5000`) — 브라우저에서 직접 플레이 가능
- 에이전트 인터페이스 API 문서
- 예제 에이전트 코드 (`random_agent.py`, `random_agent.cpp`, `greedy_agent.cpp`)
- C++ 공용 헤더 (`agent.h`) — JSON 파서, 그리드 수학, I/O 헬퍼 포함
- 로컬 실행 가이드

### 3.2 에이전트 인터페이스 (stdin/stdout JSON)

```
[시뮬레이터] → (게임 상태 JSON, 1줄) → [에이전트 프로세스]
[에이전트]   → (액션 JSON, 1줄)       → [시뮬레이터]
```

**입력 (시뮬레이터 → 에이전트):**
```json
{
  "turn": 5,
  "my_type": "SCISSORS",
  "my_pieces": [{"id": 0, "row": 3, "col": 2}],
  "enemy_pieces": [{"id": 4, "owner": "ROCK", "row": 8, "col": 1}]
}
```

**출력 (에이전트 → 시뮬레이터):**
```json
{
  "actions": [
    {"piece_id": 0, "action": "MOVE", "row": 5, "col": 3}
  ]
}
```

> - `action`: `"MOVE"` (2칸) 또는 `"CLONE"` (1칸)
> - **액션은 1개만** (첫 번째 액션만 처리, 나머지 무시)
> - 응답 시간 초과 → 빈 액션 처리 (기물 정지)

### 3.3 허용 프로그래밍 언어 ✅ 확정

| 언어 | 버전 | 비고 |
|------|------|------|
| **Python** | 3.11+ | 필수 지원 (접근성) |
| **C++** | C++17 | `g++ -std=c++17 -O2`, `agent.h` 제공 |

> Java 등 추가 언어는 추후 검토. 현재 Python/C++ 2개 언어로 운영.

### 3.4 제한 사항 ✅ 일부 확정

| 항목 | 제한 |
|------|------|
| **턴당 응답 시간** | **1000ms** |
| **한 턴 액션 수** | **1개** (기물 1개, 행동 1가지) |
| **기물 최대 수** | 플레이어당 20개 |
| **최대 턴** | 200턴 |
| 메모리 | TBD (예: 512MB) |
| 외부 네트워크 접근 | **금지** |
| 멀티스레드 | 확정 필요 |

### 3.5 웹 제출 시스템 ✅ 구현 완료

참가자는 대회 당일 웹 브라우저에서:
1. 에이전트 코드를 텍스트로 붙여넣기 (C++ 또는 Python)
2. "제출" 클릭 → 서버에서 컴파일 + 실행
3. 결과(승패/점수)와 턴별 리플레이를 브라우저에서 확인

---

## 4. 토너먼트 운영

### 4.1 권장 방식: 예선 라운드 로빈 → 결선 토너먼트

```
[예선] 전 팀 라운드 로빈 → 승점 기준 상위 8~16팀 선발
[결선] 싱글 엘리미네이션 토너먼트
[결승] 3전 2선승제 (또는 5전 3선승제)
```

| 방식 | 장점 | 단점 |
|------|------|------|
| 라운드 로빈 전체 | 공정성 최대 | 30팀 시 435경기 → 시간 과다 |
| 싱글 엘리미네이션 | 빠름 | 운 요소 큼, 1패 탈락 |
| 스위스 방식 | 균형적 | 운영 복잡 |
| **예선 RR + 결선 토너먼트** | **공정성 + 흥미** | **권장** |

> **3인 게임 특성**: 한 경기에 3팀이 동시에 대전. 경기당 결과: 1위(승), 2위(무), 3위(패) 또는 자체 기준 승점 부여 방식 확정 필요.

### 4.2 경기 수 계산 (3인 대전 기준)

| 팀 수 | 예선 경기 수 (3팀 조합) | 예상 소요 시간 (1경기 3분 기준) |
|-------|----------------------|--------------------------------|
| 25팀 | C(25,3) = 2300경기 | 병렬 실행 필수 |
| 40팀 | C(40,3) = 9880경기 | 그룹제 예선 권장 |

> **권장**: 팀을 그룹으로 나눠 그룹 내 라운드 로빈 후 각 그룹 상위팀 결선 진출

### 4.3 동점 처리 규정

1. 승점 (1위=3점, 2위=1점, 3위=0점)
2. 직접 대결 결과
3. 최종 기물 수 합산
4. 최종 동점 시: 추가 플레이오프 또는 공동 순위

---

## 5. 심사 및 채점

### 5.1 자동 채점 (주)

- 토너먼트 결과로 순위 자동 결정
- 별도 심사위원 채점 없음

### 5.2 특별상 심사 (부)

| 특별상 후보 | 선정 기준 |
|------------|-----------|
| 창의적 전략상 | 독창적인 알고리즘 접근 (코드 리뷰) |
| 최고의 역전상 | 예선 하위권 → 결선 상위 |
| 클린 코드상 | 코드 품질 및 가독성 |

> 특별상은 코드 제출 후 주최측이 코드 리뷰 → 시상식 전 발표

---

## 6. 부정행위 방지

| 유형 | 방지 방법 |
|------|-----------|
| 타 팀 코드 복사 | 유사도 분석 도구 실행 (MOSS 등) |
| 외부 통신 | 샌드박스 환경, 네트워크 차단 |
| 사전 준비 에이전트 | 게임 규칙 당일 공개로 원천 차단 |
| 의도적 타임아웃 | 응답 초과 시 자동 빈 액션 처리 |
| 악의적 코드 삽입 | 서버 샌드박스 실행 (subprocess 격리) |

---

## 7. 시뮬레이터 개발 현황 ✅

| 항목 | 상태 |
|------|------|
| 게임 엔진 (Python) | ✅ 완료 |
| 에이전트 프로토콜 (stdin/stdout JSON) | ✅ 완료 |
| 예제 에이전트 (Python / C++) | ✅ 완료 |
| 웹 기반 플레이 UI | ✅ 완료 |
| 웹 코드 제출 + 리플레이 뷰어 | ✅ 완료 |
| 리플레이 저장 (JSONL) | ✅ 완료 |
| 엔진 단위 테스트 | ✅ 11/11 PASS |
| 토너먼트 자동 운영 시스템 | 🔲 미구현 (대회 전 개발 필요) |
| 리더보드 | 🔲 미구현 |

---

## 8. 미결 사항

| # | 항목 | 상태 |
|---|------|------|
| 1 | 허용 프로그래밍 언어 | ✅ Python / C++ |
| 2 | 게임 유형 | ✅ 동시가위바위보 (영토 확장형) |
| 3 | 턴당 응답 시간 | ✅ 1000ms |
| 4 | 한 턴 액션 수 | ✅ 1개 (기물 1개) |
| 5 | 시작 기물 수 | ✅ 플레이어당 4개 |
| 6 | 셀 점유 제한 | ✅ 셀당 1기물 |
| 7 | 토너먼트 방식 | 🔲 미정 (그룹 예선 권장) |
| 8 | 3인 대전 승점 방식 | 🔲 미정 |
| 9 | 메모리 제한 | 🔲 미정 |
| 10 | 멀티스레드 허용 여부 | 🔲 미정 |
| 11 | 사전 학습 모델 허용 여부 | 🔲 미정 |
| 12 | 특별상 종류 및 수 | 🔲 미정 |

---

> **다음 단계**: 토너먼트 자동 운영 시스템 개발 → 부하 테스트 (병렬 실행)
